# Quarkus Configuration
quarkus.application.name=oracle-mcp-server
quarkus.http.port=8080

# Logging Configuration
quarkus.log.level=INFO
quarkus.log.category."com.example.mcp".level=DEBUG
quarkus.log.console.format=%d{yyyy-MM-dd HH:mm:ss,SSS} %-5p [%c{3.}] (%t) %s%e%n

# Jackson Configuration
quarkus.jackson.serialization-inclusion=non-null
quarkus.jackson.fail-on-unknown-properties=false

# LangChain4J Configuration - Multiple LLM Provider Support
# The application supports multiple LLM providers and auto-detects which one to use
# based on environment variables. Set one of the following:

# Option 1: OpenAI (GPT-4, GPT-3.5, etc.)
# Set environment variable: OPENAI_API_KEY=sk-...
# quarkus.langchain4j.openai.api-key=${OPENAI_API_KEY}
# quarkus.langchain4j.openai.chat-model.model-name=gpt-4
# quarkus.langchain4j.openai.timeout=60s

# Option 2: Google Gemini (Gemini Pro, etc.)
# Set environment variable: GEMINI_API_KEY=...
# Default model: gemini-pro

# Option 3: Anthropic Claude (Coming Soon)
# Set environment variable: ANTHROPIC_API_KEY=...

# Auto-detection priority: OpenAI > Gemini > Anthropic
# If multiple API keys are set, OpenAI will be used by default

# CORS Configuration (if needed for web clients)
quarkus.http.cors=true
quarkus.http.cors.origins=*
quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with

# Development mode settings
%dev.quarkus.log.level=DEBUG
%dev.quarkus.http.port=8080

# Production mode settings
%prod.quarkus.log.level=INFO
